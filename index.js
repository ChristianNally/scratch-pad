
/**
 * sum function definition. This function takes in no parameters, but it adds two numbers and return the sum.
 * @returns {Number} sum
 */
const sum = function() {
  let a = 10;
  let b = 20;
  sum = a + b;
  return sum
}
console.log(sum);